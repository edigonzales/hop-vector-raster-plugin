name: Publish Maven release

on:
  push:
    tags: ['v*']

permissions:
  contents: read

jobs:
  verify:
    uses: ./.github/workflows/verify.yml
  publish:
    needs: verify
    uses: edigonzales/hop-plugin-ci/.github/workflows/plugin-publish.yml@e3985b7ab27d22cc63d9d9bf206d0ffdd118c38e
    with:
      ci-ref: e3985b7ab27d22cc63d9d9bf206d0ffdd118c38e
      repository-kind: release
      verified-artifact-name: hop-vector-raster-plugin-canonical
      pom-file: assemblies/assemblies-hop-vector-raster/pom.xml
      artifact-id: hop-vector-raster-plugin
      plugin-root: plugins/transforms/vector-raster
    secrets:
      MAVEN_USERNAME: ${{ secrets.MAVEN_USERNAME }}
      MAVEN_PASSWORD: ${{ secrets.MAVEN_PASSWORD }}
