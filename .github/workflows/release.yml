name: Release

on:
  push:
    branches:
      - "main"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    name: Build release ZIP
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"
          cache: maven

      - name: Build and test plugin
        shell: bash
        run: mvn -U -B -ntp clean verify

      - name: Validate packaged ZIP
        id: asset
        shell: bash
        run: |
          set -euo pipefail
          python3 scripts/check-distribution.py
          mapfile -t zip_files < <(find assemblies/assemblies-hop-vector-raster/target -maxdepth 1 -type f -name 'hop-vector-raster-plugin-*.zip' | sort)

          if [[ ${#zip_files[@]} -ne 1 ]]; then
            echo "Expected exactly 1 vector/raster plugin ZIP, found ${#zip_files[@]}" >&2
            printf '%s\n' "${zip_files[@]}"
            exit 1
          fi

          zip_path="${zip_files[0]}"
          zip_name="$(basename "$zip_path")"
          echo "zip_path=$zip_path" >> "$GITHUB_OUTPUT"
          echo "zip_name=$zip_name" >> "$GITHUB_OUTPUT"
          ls -lh "$zip_path"

      - name: Upload ZIP artifact
        uses: actions/upload-artifact@v4
        with:
          name: release-zip
          if-no-files-found: error
          path: ${{ steps.asset.outputs.zip_path }}

  release:
    name: Create release
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Download ZIP artifact
        uses: actions/download-artifact@v4
        with:
          name: release-zip
          path: dist

      - name: Collect ZIP asset
        id: assets
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p release-assets
          mapfile -t zip_files < <(find dist -type f -name 'hop-vector-raster-plugin-*.zip' | sort)

          if [[ ${#zip_files[@]} -ne 1 ]]; then
            echo "Expected exactly 1 vector/raster plugin ZIP under dist/, found ${#zip_files[@]}" >&2
            find dist -maxdepth 4 -type f -print || true
            exit 1
          fi

          zip_path="${zip_files[0]}"
          zip_name="$(basename "$zip_path")"
          cp "$zip_path" "release-assets/$zip_name"
          echo "zip_name=$zip_name" >> "$GITHUB_OUTPUT"

      - name: Resolve release metadata
        id: meta
        shell: bash
        run: |
          set -euo pipefail
          zip_name="${{ steps.assets.outputs.zip_name }}"
          version="$(echo "$zip_name" | sed -E 's/^hop-vector-raster-plugin-([0-9A-Za-z.+_-]+)\.zip$/\1/')"
          if [[ -z "$version" || "$version" == "$zip_name" ]]; then
            echo "Could not resolve version from ZIP filename: $zip_name" >&2
            exit 1
          fi

          safe_version="${version//+/-}"
          timestamp="$(date -u +%Y%m%d-%H%M)"
          short_sha="${GITHUB_SHA::7}"
          echo "tag=auto-v${safe_version}-${timestamp}-${short_sha}" >> "$GITHUB_OUTPUT"
          echo "name=Auto Release ${version} (${timestamp} UTC)" >> "$GITHUB_OUTPUT"

      - name: Create GitHub release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.meta.outputs.tag }}
          target_commitish: ${{ github.sha }}
          name: ${{ steps.meta.outputs.name }}
          draft: false
          prerelease: false
          generate_release_notes: true
          files: release-assets/*.zip
